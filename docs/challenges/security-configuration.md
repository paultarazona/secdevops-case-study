# Security Configuration

Compare V1's open CORS, missing headers, unlimited authentication attempts, and
error-detail exposure with V2's explicit CORS, Helmet, rate limiting, and
centralized error handling.
